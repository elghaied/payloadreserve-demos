{
  "name": "payload-reserve-demos",
  "private": true,
  "scripts": {
    "dev:salon": "turbo dev --filter=salon",
    "dev:landing": "turbo dev --filter=landing",
    "dev:docs": "turbo dev --filter=docs",
    "dev:hotel": "turbo dev --filter=hotel",
    "dev:restaurant": "turbo dev --filter=restaurant",
    "dev:events": "turbo dev --filter=events",
    "docker:salon": "bash scripts/stripe-listen-docker.sh salon 3000",
    "build:salon": "turbo build --filter=salon",
    "build:hotel": "turbo build --filter=hotel",
    "build:restaurant": "turbo build --filter=restaurant",
    "build:events": "turbo build --filter=events",
    "build:landing": "turbo build --filter=landing",
    "build:docs": "turbo build --filter=docs",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "generate:types": "turbo generate:types",
    "generate:importmap": "turbo generate:importmap"
  },
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": "^18.20.2 || >=20.9.0",
    "pnpm": "^9 || ^10"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild",
      "sharp",
      "unrs-resolver"
    ],
    "overrides": {
      "payload": "3.79.0"
    }
  }
}
